meta {
  name: Data-Constants-Divisions
  type: http
  seq: 8
}

get {
  url: {{base_url}}/data/constants/divisions
  body: none
  auth: inherit
}

docs {
  # Data-Constants-Divisions

  Constant; returned directly as an array of objects
}
