meta {
  name: Data-Constants-Categories
  type: http
  seq: 7
}

get {
  url: {{base_url}}/data/constants/categories
  body: none
  auth: inherit
}

docs {
  # Data-Constants-Categories

  Constant; returned directly as an array of objects
}
